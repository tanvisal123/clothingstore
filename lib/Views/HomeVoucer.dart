import 'package:flutter/material.dart';
class HomeVoucer extends StatelessWidget {
  const HomeVoucer({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('HomeVoucer'),
      ),
    );
  }
}